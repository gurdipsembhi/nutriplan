{
  "name": "nutriplan",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "seed": "npx ts-node -P server/tsconfig.json server/src/scripts/seedFoods.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
